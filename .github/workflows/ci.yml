name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  redirect:
    name: Package VS Code VSIX
    runs-on: ubuntu-latest
    steps:
      - run: echo "ide-support is a redirect — packages live in almasix-vscode / almasix-idea"

  jetbrains:
    name: Package JetBrains plugin
    runs-on: ubuntu-latest
    steps:
      - run: echo "ide-support is a redirect — packages live in almasix-vscode / almasix-idea"
